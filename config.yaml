num_clients: 2
num_rounds: 10
model: xgb # xgb, logistic_regression
dataset: kaggle_hf # mnist, cvd, libsvm, kaggle_hf
data_path: dataset/

experiment:
  name: "test_run_1"
  results_path: "results"
  debug: True

xgb:
  task_type: BINARY
  batch_size: 32
  num_iterations: 100
  tree_num: 500

production_mode: True # Turn on to use environment variables such as data path, server address, certificates etc.
